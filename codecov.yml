# Codecov 設定
# カバレッジ計測を導入したばかりのため、ステータスは情報提供のみ (非ブロッキング) とする。
coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

comment:
  layout: 'reach, diff, files'
  require_changes: false

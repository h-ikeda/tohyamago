# eslint-plugin-react (peer: eslint <=9.7) と eslint-plugin-jsx-a11y (peer: eslint <=9)
# は ESLint 10 を peerDependencies に明記していないが、実行時は ESLint 10 で動作する。
# 正式対応リリースが出るまでの暫定措置として peer 競合を許容する。
legacy-peer-deps=true
